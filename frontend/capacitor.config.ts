import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.andrewtimmer.investingapp',
  appName: 'InvestingApp',
  webDir: 'www'
};

export default config;
