
import { SurveyComponent } from './survey.component';

describe('SurveyComponent', () => {
  it('should create an instance', () => {
    const component = new SurveyComponent();
    expect(component).toBeTruthy();
  });
});
