
import { LinkbankComponent } from './linkbank.component';

describe('LinkbankComponent', () => {
  it('should create an instance', () => {
    const component = new LinkbankComponent();
    expect(component).toBeTruthy();
  });
});
