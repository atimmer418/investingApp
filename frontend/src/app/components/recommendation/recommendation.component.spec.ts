
import { RecommendationComponent } from './recommendation.component';

describe('RecommendationComponent', () => {
  it('should create an instance', () => {
    const component = new RecommendationComponent();
    expect(component).toBeTruthy();
  });
});
