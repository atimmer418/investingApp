export const environment = {
    production: false, // It's a test build, not a full production build
    backendApiUrl: 'https://api-dev.fredvested.com/api',
    rpId: 'app-dev.fredvested.com' // Your test frontend/app origin hostname
  };