# FRED Builder Agent Findings

- 2026-05-30 — build/browserslist — `.browserslistrc` had `iOS >=14` / `Safari >=14` which causes esbuild destructuring errors with Angular 19.2; raising to `>=16` fixes it (FRED targets iOS 16+)
- 2026-05-30 — npm/esbuild — `npm audit fix` can downgrade esbuild (0.28→0.25) which breaks the build; pin esbuild at 0.28.x with explicit `npm install esbuild@0.28.0 --save-dev` after any audit fix run
- 2026-05-30 — mfu — `MonthlyFreedomUpdateData.equityLevel` (1-6) is already computed server-side and available via `checkShouldShow()` — use it for pig avatar level without an extra API call
- 2026-05-30 — angular — after B6 overhaul, ~50 standalone component imports across 20+ files were stale; angular-compiler TS-998113 warnings are the canonical signal to clean them
- 2026-05-30 — typescript — dynamic `import('module-name')` even cast `as any` still triggers TS 2307 if the module has no resolvable types; `// @ts-ignore` on the import line is the minimal fix
- 2026-05-30 — User.java — Lombok @Getter/@Setter on class auto-generates getters for all private fields; only manually-added inline getters (e.g., from Bucket C merge) duplicate what Lombok already provides — watch for duplicates
- 2026-05-30 — ITPM/today.html — dollar sign in date-derived password must use String.fromCharCode(36) not a template literal to avoid any shell/interpolation stripping when the ITPM skill writes the file via heredoc or script
- 2026-05-30 — sse-streaming — Spring SseEmitter emits `data:<json>\n\n` WITHOUT a space after the colon; frontend parsers that gate on `line.startsWith('data: ')` (with a space) will silently drop every event (chat.service.ts:83)
- 2026-05-30 — chat/stream — `GET /api/chat/stream` returns HTTP 500 when `userId` is null because `userRepository.findById(null)` throws; frontend must always supply userId (validated via fetch test against running backend)
- 2026-05-30 — ionic/nav — `NavController` is a service from `@ionic/angular` (not standalone), injected via constructor only — never add it to `imports: []` in a standalone component; use `navigateBack(route)` for back animations
- 2026-05-30 — ionic/nav — `.section-card { padding: 16px }` is a global rule; cards that need edge-bleeding content (e.g. absolutely-positioned images) must explicitly override `padding: 0` and give their inner content wrapper its own `padding: 16px`
- 2026-05-30 — sass/deprecation — `darken()`/`lighten()` in component SCSS must be replaced with `color.adjust($c, $lightness: ±N%)` after adding `@use 'sass:color'` as the first line (before any `@import url(...)`);
- 2026-06-06 — plaid/change-bank — Backend GET /plaid/primary-bank-account returns field `accountSubtype` (lowercase t); frontend reads `accountSubType` (capital T). Pre-existing case mismatch (also in unchanged TS formatAccountDisplay); subtype always falls back to 'Checking'. Not introduced by FRED-124 but worth fixing.
- 2026-06-06 — verify/env — Angular CLI + @angular/compiler-cli are NOT installed in the verifier sandbox, so `ng build`/template type-checking can't run; only raw `tsc --noEmit` is available (emits 2 pre-existing tsconfig deprecation warnings, no module-resolution errors). Backend on :8080 and local.fredvested.com are unreachable (host-allowlist block), so Phases 2 & 3 cannot execute here.
- 2026-06-06 — frontend/test-runner — security `overrides` in package.json (`glob: ">=11.1.0"`, `minimatch: ">=10.2.5"`) force karma@6.4.4 onto incompatible majors → `ng test` crashes at karma `file-list.js:74` (glob v13 dropped sync `.found`; minimatch v10 default export is an object, not callable). Fix: nested override `"karma": {"glob":"^7.1.7","minimatch":"^3.0.4"}` then `npm install --legacy-peer-deps`. karma is a devDependency only, so glob7.2.3/minimatch3.1.5 (both patched) carry no production risk.
- 2026-06-06 — npm/install — repo has a pre-existing peer conflict: `@capacitor/push-notifications@8.1.1` needs `@capacitor/core@>=8`, but the repo pins `@capacitor/core@7.2.0`. `npm install` ERESOLVE-fails without `--legacy-peer-deps`. Always install this frontend with `--legacy-peer-deps`.
- 2026-06-06 — frontend/test-runner — verifier/CI must run a single spec headless: `ng test --include='**/<name>.spec.ts' --watch=false --browsers=ChromeHeadless`. Whole-suite `ng test` may surface legacy CLI-generated specs that are red; target the spec under test.
- 2026-06-06 — backend/tests — `./gradlew compileTestJava` works; `./gradlew test` runs JUnit 5 (useJUnitPlatform). The only existing test, `BackendApplicationTests`, is `@SpringBootTest` and FAILS without MySQL (DataSourceBeanCreationException). Builder-written `backend-unit` tests MUST be plain JUnit (no `@SpringBootTest`) so they need no DB; verifier runs them targeted via `./gradlew test --tests <FullyQualifiedClass>`.
- 2026-06-22 — scss/partial — `_blue-hero-header.scss` emits bare selectors (`.back-btn`, `.header-title`, `ion-toolbar`, etc.), not mixins. When `@use`d in a component that already defines those classes, the component's own later rules win the cascade. Any selectors that must remain active inside the hero block need explicit scoped overrides (e.g. `.blue-hero-header .back-btn { color: #fff }`) after the component's bare rules.
- 2026-06-13 — tab3/scss — `npx tsc --noEmit` in frontend/ always emits TS5101/TS5107 (baseUrl and moduleResolution=node10 deprecated in TS7) regardless of HTML/SCSS changes — these are pre-existing tsconfig.json deprecation warnings, not compilation failures introduced by page-level work.
- 2026-06-07 — ionic/safe-area — custom `div` headers inside `ion-header` (no `ion-toolbar`) do NOT automatically receive Ionic's safe-area padding. Must add `padding-top: calc(env(safe-area-inset-top, 0px) + Npx)` explicitly (or the global `safe-area-top` class, which uses `!important` and loses any breathing gap).
- 2026-06-06 — verifier — `/code-review` invoked from inside the verifier SUBAGENT is UNVERIFIED (may hit nested-agent limits). The verifier prompt is defensive: the INLINE code-review is the reliable path the gate depends on; the `/code-review` skill call is best-effort ("IF you can invoke"). Subagents can invoke skills generally (builder-agent already invokes brainstorming/writing-plans), so the open question is specifically whether nested `/code-review` spawns. Confirm on the first real verifier run; if it errors noisily, soften the verifier prompt to inline-only.
- 2026-06-07 — verifier/safe-area-lint — `node test/ui/safe-area-lint.mjs --files=<scss>` is the runnable static floor for the FRED-124 notch class; it passed on the change-bank-account fix (`.blue-hero-header` inside `ion-header` now uses `calc(env(safe-area-inset-top,0px)+10px)`), matching the documented 2026-06-07 ionic/safe-area convention for custom div headers.
- 2026-06-13 — verifier/safe-area-lint — safe-area-lint.mjs flags `.custom-profile-header` (tab3 Option B) as an offender, but this is a FALSE POSITIVE: the div carries the global `.safe-area-top` class (global.scss:535 `padding-top: env(safe-area-inset-top,0px) !important`) which the linter cannot see — it only parses the component SCSS. When a header's safe-area comes from a global utility class, cross-check global.scss before treating the lint exit-1 as an in-scope fail.
- 2026-06-13 — verifier/env — Phase 3 UI (live render/screenshots/network) is NOT executable in this verifier sandbox: no built `frontend/www` and no `serve` bin (so `npm run start` fails), and no Chrome/computer-use MCP tools are present. ui-acceptance ACs can only be verified at the static floor (SCSS/HTML/global.scss + safe-area-lint) here; device-size screenshots and click-through must be deferred to an environment with the browser tools.
- 2026-06-13 — auth.service — `progress.retirementIncome` from UserProgress is stored as ANNUAL in the backend (confirmed by my-profile.page.ts:158 comment "Income is stored as Annual in backend"). The 4%-SWR formula is `annualIncome / 0.04` directly — do NOT multiply by 12 before dividing.
- 2026-06-13 — tab3/stat-strip — header bottom-padding must be at least `strip_height + card_overlap + buffer` (e.g. 40px with a 58px strip and 20px overlap gives 20px clearance). Increasing from 36px to 40px was sufficient for the strip to remain fully visible above the overlapping white card.
- 2026-06-13 — tab3/safe-area — safe-area-lint.mjs exit-1 on tab3 .custom-profile-header is a known false positive: the header clears the notch via the `.safe-area-top` global utility class (global.scss:535-537, padding-top:env(safe-area-inset-top)) applied in HTML, which the component-SCSS-only linter cannot see. Verify the global utility before treating as a real offender.
- 2026-06-13 — retirementIncome — UserProgress.retirementIncome is stored ANNUAL in backend (my-profile.page.ts:158-161). Portfolio goal = annualIncome / 0.04 (SAFE_WITHDRAWAL_RATE) with NO extra ×12. Verifier should reject any dollars-away/goal formula that multiplies by 12 again.
- 2026-06-13 — verifier/FRED-192 — Stat-strip rework verified APPROVED at static floor: tab3.page.ts diff is purely additive (1 trailing-comma line + 128 adds), tsc baseline-stash confirms ZERO new errors beyond TS5101/TS5107, all three service methods (getUserProgress/getPortfolioDashboard/getKycData) exist with matching field shapes, dollars-away uses annual/0.04 (no ×12), loading+error fallbacks all yield '—'/'$0' (never NaN/undefined) with takeUntil(destroy$) + error handlers on all 3 subs.
- 2026-06-14 — verifier/safe-area-lint — FIXED the global-utility-class blind spot: linter now treats an ion-header div carrying .safe-area-top (GLOBAL_SAFE_TOP_CLASSES) as safe, so it no longer false-positives tab3's .custom-profile-header.
- 2026-06-14 — verifier/auth.service.promptForPasskeyReauth — cold-start passkey-reauth flag-reset trace: ALL 6 exit paths after reAuthInProgressSubject.next(true) reset to false (!keychainEmail / start-catch / allowedCreds==0 / finish-success / finish-non-success / outer-catch). KEY: the one await OUTSIDE the try (keychainSyncService.getAccountEmail, between next(true) and the try) is reject-safe — keychain-sync.service.ts:63-78 has an internal try/catch returning null — so no uncaught-rejection path can strand the flag true. Race-fix is real: app.component.ts:211 filter `if(!isLoggedIn && reAuthInProgress) return false` gates get-started, and the stream has debounceTime(300), so moving next(true) before the first await closes the window. clearAccountEmail fully removed from auth.service (grep=0). timeout imported rxjs line 3, tsc --noEmit EXIT 0.
- 2026-06-14 — verifier/scope-drift — prompt asserted "only auth.service.ts changed (+ unrelated verifier-agent.md)" but working tree ALSO had tab3.page.html + tab3.page.scss modified, and verifier-agent.md was NOT modified. Always run `git status --porcelain` first and trust it over the prompt's scope claim. tab3 SCSS carried suspect values (header-overlap height:800px/margin-top:-780px) — out of scope for the auth fix but worth a backlog note.
- 2026-06-14 — auth/nav — transient-retry + _source tagging pattern: rxjs `retry({count,delay})` with a `throwError` re-throw for non-transient statuses is the canonical guard for DB-load resilience; `_source:'db'|'localStorageFallback'` on UserProgress lets app.component.ts hold an authenticated user on their current route rather than ejecting to /get-started when the backend is transiently unreachable.
- 2026-06-14 — verifier/loadUserProgress — APPROVED at static floor. rxjs 7.8.2 `retry({count:2,delay})` = 1 initial + 2 retries (3 attempts); delay cb `attempt` is 1-based → backoff 400ms/800ms matches `400*2^(attempt-1)`. `timeout(10_000)` is UPSTREAM of retry so it re-applies per attempt. delay-returns-`throwError` correctly aborts retry on non-transient (status!==0 && <500 && !TimeoutError → 401/403/404 don't retry). `_source` leak-free: both updateProgress callers (surveyinitial:159, auth.service:331) build fresh partials, never spread full progress. distinctUntilChanged JSON.stringify now includes _source so db→fallback flip is seen as a change (not swallowed); no nav loop since holdAuthenticatedUserOnFallback early-returns on same-route.
- 2026-06-14 — verifier/latency-risk — pure-hang worst case for loadUserProgress is 3×timeout(10s)+1.2s backoff ≈ 31.2s before tagged fallback emits, but the 12s emergency cover-hide (app.component.ts:143, independent setTimeout in ngOnInit) and AppLockService 10s init watchdog both force-hide covers FIRST, so the cover is never stuck >12s. Acceptable: cold-start userProgressSubject stays null (constructor default, line 57) during retries so nav filter (progress!==null, app.component.ts:215) defers — no premature /get-started.
- 2026-06-14 — verifier/scope-drift — RECURRING (3rd time): prompt said "only these two files changed" but auth.service.ts ALSO had an undescribed promptForPasskeyReauth() change (lines ~407-445: keep Keychain anchor on transient start-failure + move reAuthInProgress flag before first await). Working tree also carried out-of-scope tab3.page.* + change-bank-account.page.scss. Always diff the full file, not just the prompt-named hunks — builder/prompt scope claims under-report.
- 2026-06-14 — assets/svg-viewbox — All 5 FREDpig range SVGs have square viewBox (~850×850), so they render cleanly inside ion-avatar (circular crop) with no distortion. The small decimal variance (850.39368 vs 850.4) is negligible for CSS layout.
- 2026-06-14 — equity-pig-utils — EquityPigUtils static class in frontend/src/app/utils/ exposes two methods: pigSrcFromEquity(number|null|undefined) for live dollar values and pigSrcFromLevel(1-6) for backend equityLevel. L5&L6 both map to range 5 via Math.min(level, 5). Zero/null/undefined/NaN always yield pig-range-1.svg (no broken img).
- 2026-06-14 — verifier/FRED-195 — APPROVED with full runtime evidence. Live render confirmed: facebook@gmail.com summary.equity=$615.55 → BOTH tab3 (.user-avatar) and my-profile avatars render pig-range-1.svg (consistent for same user); GET /api/portfolio/dashboard 200, console clean, old ionicframework.com placeholder removed (hasIonicPlaceholder=false). KEY consistency proof: my-profile.page.ts:235 assigns currentPortfolioValue = data.summary.equity (SAME field tab3:373 uses for liveEquity), so AC#4/#6 hold — currentPortfolioValue IS summary.equity, not a holdings-only figure. Boundary tests exact-match backend calculateEquityLevel ($1000→r2, $1m→r5, L5&L6→r5, null/NaN/neg→r1). tsc --noEmit EXIT 0. md5(pig-range-1.svg)==md5(source pig$0-$1k.svg), NOT blackandwhiteversion.
- 2026-06-14 — verifier/scope-drift — RECURRING (4th time): FRED-195 working tree also carried OUT-OF-SCOPE changes unrelated to the pig avatar: tab3.page.scss/html scroll-wrapper restructuring (.scroll-inner, gradient background, card border 1px→2px, margin-top -14→-18px), footer text "FREDvested"→"FREDvested LLC", plus app.component.ts/auth.service.ts/change-bank-account.page.scss/skills/backlog edits. None affect the FRED-195 ACs but a verifier must diff the FULL working tree (git status --porcelain) and isolate the story's files — never trust the prompt's file list.
- 2026-06-15 — angular/karma — dynamic `await import('rxjs')` inside a `fakeAsync` Karma test body triggers ChunkLoadError (webpack lazy-chunk timeout). All rxjs imports in spec files must be top-level static imports, never dynamic.
- 2026-06-15 — karma/sandbox — Karma's ChromeHeadless launcher needs an absolute CHROME_BIN (e.g. /opt/pw-browsers/chromium-1194/chrome-linux/chrome); `which chromium` may return empty and `CHROME_BIN=$(which chromium)` then fails to launch. Export the absolute path before `ng test`. The ChromeHeadlessNoSandbox launcher (--no-sandbox --disable-gpu) in karma.conf.js is required to run as root in the sandbox.
- 2026-06-15 — rxjs/retry — FRED's canonical transient-only retry policy lives in auth.service.ts loadUserProgress() (~line 250): timeout(10_000) + retry({count:2, delay: transient? timer(400*2^(attempt-1)) : throwError}) where transient = TimeoutError || status===0 || status>=500. tab3 KYC (FRED-196) mirrors it exactly. Reuse this block verbatim for any flaky-call hardening.
- 2026-06-15 — dark-mode/scss — When removing dark-mode SCSS, always grep the full component SCSS for variable names that appear ONLY in the deleted @media block (e.g. `$background-dark`) — they become orphaned and must be removed too per the no-dead-code rule.
2026-06-15 — frontend/theme — Removing `@media (prefers-color-scheme: dark)` blocks does NOT disable the kept FRED-146 `auto`-mode listener (settings.service.ts applyTheme L108-110), which still toggles `body.dark` under OS dark mode; light-only is safe ONLY because zero `.dark`/`body.dark` SCSS selectors remain — always grep `(body\.dark|\.dark)` in *.scss when verifying dark-mode removal, not just `prefers-color-scheme`.
2026-06-15 — frontend/theme — Dark-mode removal: settings.service.ts applyTheme('auto') still toggles a body.dark class, but with all @media(prefers-color-scheme:dark) blocks + every .dark/body.dark SCSS selector gone, that class is inert (no CSS keyed to it). Verifying "light-only" = grep for both the @media blocks AND any .dark selector, not just the media query.
2026-06-15 — frontend/global.scss — global.scss ships a pre-existing commented-out Ionic dark-palette @import scaffold ("/* @import ...dark... */" + "Dark mode disabled" comment); not builder-introduced — don't flag it as a leftover dark block in AC-4-style clean-removal checks.
2026-06-16 — sass/@use — `@use` rules must appear as the FIRST statement in a SCSS file (before `:host`, `ion-content`, or any other rule). Put the `@use` line at line 1, then `:host {}` etc. below. Sass enforces this strictly and errors with "must be written before any other rules."
2026-06-16 — sass/paths — Relative `@use` path depth from a SCSS file to `src/theme/`: pages in `src/app/{page}/` need `../../theme/`, pages in `src/app/{subdir}/{page}/` need `../../../theme/`. Count the depth from the file's directory to `src/`, then append `theme/`.
- 2026-06-16 — scss/shared-partial — FRED-194: extracting a repeated header into `theme/_blue-hero-header.scss` and `@use`-ing it requires correct relative depth (`../../` for app/<page>, `../../../` for app/pages/* and app/components/*); verify with `npx sass <file> /dev/stdout` (exit 0 + emits gradient) since a wrong path fails only at build time. The gradient literal `linear-gradient(90deg,#2a5ae0,#1d4ed8)` still lives in tab3.page.scss (parent tab) — true single-source would fold tab3 in too (out of scope here).
- 2026-06-18 — security/pin — fail-open gate: PinService.hasPin() localStorage fallback uses key `stepUpEnabled:<userId>` (scoped per user). JwtTokenUtils.clearJwtData() must clear the marker BEFORE removing userId from localStorage — derive the key first, then removeItem, then clear userId. Auth.service.logout() calls clearJwtData() so no separate clear call is needed there.
- 2026-06-18 — scss/edit-pattern — When inserting a block BEFORE an existing CSS rule using the Edit tool, include the `.selector {` opening brace of the existing rule in `old_string` — otherwise the edit silently consumes the selector line, producing a dangling body block (SCSS parse error "expected {").
- 2026-06-18 — security-settings/three-state — three-state gating (`checkingStepUp` / overlay / loaded) requires two separate `*ngIf` guards on the overlay div: `*ngIf="!checkingStepUp && !isAuthenticated"`. A single `!isAuthenticated` guard (the original) conflates the checking and verify states, causing the overlay to flash on every load.
- 2026-06-18 — verifier/sandbox — local.fredvested.com tunnel can answer TLS at Cloudflare edge yet return `403 host_not_allowed` (x-deny-reason header) when no dev-server origin is connected; a bare curl 200/connect check is NOT proof the Angular app renders — confirm a real HTML/app-root body before claiming ui-acceptance device render
- 2026-06-18 — verifier/karma — sandbox has no `ng` on PATH and CHROME_BIN unset, but `node_modules/.bin/ng` + Playwright Chromium at /opt/pw-browsers/chromium-1194/chrome-linux/chrome work with `--browsers=ChromeHeadlessNoSandbox`; scoped specs run locally this way instead of deferring to CI
- 2026-06-18 — verifier/safe-area-lint — safe-area-lint.mjs flags any bespoke `div` header inside ion-header even when the safe-area padding lives in a shared `@use`'d theme partial (e.g. _blue-hero-header.scss `padding: calc(env(safe-area-inset-top,0px)+2px)`); resolve by reading the imported partial before treating as a FRED-124 offender
- 2026-06-19 — angular/signals — Angular 19 `toSignal()` requires an injection context; calling it from a service constructor is safe (constructor runs in the DI context). Use `{ initialValue: null }` when bridging a BehaviorSubject whose first emission isn't guaranteed synchronous.
- 2026-06-19 — angular/signals — Angular `computed()` is LAZY — it only evaluates when read. Side effects inside computed (e.g., `localStorage.setItem`) only fire after a consumer calls the signal. In specs, always read `svc.stats()` before checking localStorage state.
- 2026-06-19 — angular/signals — `effect()` in a component constructor requires Angular 19+ (no `DestroyRef` injection needed); the effect auto-cleans up when the component is destroyed. Use it to bridge signal values back to plain class properties (e.g., keeping `liveEquity` a plain number for template binding).
- 2026-06-19 — jwt-token.utils — clearJwtData() pattern: always read/use userId BEFORE calling removeItem('userId'). FRED-199 lesson: per-user scoped localStorage keys must be cleared in the same block, before the userId anchor is removed.
- 2026-06-19 — tab3/stat-strip — Moving stat-strip logic to a singleton service means the old FRED-196 tab3.page.spec.ts (testing component properties like `freedomAge`) must be replaced; service specs test the math, component specs test the render-from-signal contract.
- 2026-06-19 — angular/signals — builder put a localStorage write (_persistSnapshot) INSIDE a computed() (freedom-stats.service.ts:164). It fires here only because tab3's template reads stats() (active consumer), but computed() is meant to be PURE — side effects can skip when unsubscribed / break under future strict mode. Idiomatic fix is effect(). Watch for this pattern in signal-based services.
- 2026-06-19 — plaid/field-casing — Backend PlaidController returns `accountSubtype` (lowercase t); any frontend read must use the exact same casing. A capital-T `accountSubType` silently reads as undefined and triggers the fallback. When adding fallbacks for missing Plaid subtypes, use "Account" (neutral) not "Checking" (biased) — matches plaid.service.ts:80 convention.
2026-06-19 — frontend/change-bank-account — formatAccountDisplay() is dead code (0 callers); the rendered subtype comes from the template `.page.html` inline binding (`accountSubtype || 'Checking'`). Fixing the .ts method does NOT change the UI. Verifier must confirm the *template* renders the value, not just the component method, when the AC is user-visible.
- 2026-06-19 — plaid/change-bank — FRED-189 round 2: render-path fix lands as `formatAccountSubtype()` bound at change-bank-account.page.html:37 (active card) + :12 (commented hero); reads lowercase `accountSubtype`, title-cases, falls back to neutral 'Account'. Builder's round-1 mistake (fixing a zero-caller helper while the template kept `|| 'Checking'`) is the canonical "dead-code fix" pattern — always grep the helper for template callers before passing a UI AC.
2026-06-20 — auth/app-lock — JWT sliding-refresh idle-cap review (auth.service.refreshToken + app-lock.service): finalize() is the single latch-reset (runs on success/error/timeout/unsubscribe); lastInteractionTime updated ONLY by registerUserInteraction (1 caller: app.component throttled HostListener) + unlock — verified no HTTP/background path resets it; activityInterceptorFn/withInterceptors fully removed; tsc -p tsconfig.app.json exit 0. Clean PASS.
- 2026-06-22 — scss/safe-area — when a shared blue-hero header is brought in via `@use '...blue-hero-header'`, the safe-area-inset-top padding lives in the PARTIAL (.blue-hero-header padding: calc(env(safe-area-inset-top..))), so test/ui/safe-area-lint.mjs (which doesn't follow @use and only suppresses `.page.html` siblings, not `.component.html`) reports a false-positive FRED-124 advisory — verify the partial, don't treat the lint flag as a real notch defect
- 2026-06-22 — scss/cascade — KYC editMode reuses the blue partial which emits bare `ion-toolbar{--background:transparent}`; because `@use` is placed at the top, the component's later `ion-toolbar{--background:#f8fafc}` wins by source order (equal specificity) and the onboarding header is not leaked — order of @use vs component rules matters for no-leak verification
- 2026-06-23 — scss/shared-mixin — FRED-202: defining shared form-field styles as a `@mixin form-fields-scoped()` in a theme partial and calling `@include ff.form-fields-scoped()` inside each component's `:host {}` is the correct scoping pattern — the alternative of top-level classes in the partial would bleed globally if ever `@forward`ed or `@import`ed
- 2026-06-23 — ionic/native-input — Converting `ion-input` to native `<input>` in Ionic standalone: remove `IonInput` from the component's `imports: []`; switch `(ionInput)`/`(ionChange)` handlers to `(input)` and read `(event.target as HTMLInputElement).value` (not `event.detail.value`); for masked/formatted fields, also re-assign `(event.target as HTMLInputElement).value = formatted` since native input has no `value` setter on the ion-input wrapper
- 2026-06-23 — build/pre-existing-error — `kyc-verification.component.scss` has a pre-existing `@use '../../theme/blue-hero-header'` that fails at `ng build` (Can't find stylesheet to import); this error exists on the clean develop branch and is NOT introduced by any FRED-202 changes — always stash-verify before attributing a build error to a story
- 2026-06-23 — frontend/forms — Converting ion-input→native input with a custom formatter handler (SSN/phone): must drop [(ngModel)] for [value], read event.target.value (not event.detail.value), AND write the reformatted string back to event.target.value or the displayed value won't update. add-beneficiary FRED-202 did this correctly.
- 2026-06-23 — verify/build-attribution — IonToolbar-unused warnings on FRED-202 pages were pre-existing (templates had 0 ion-toolbar before the diff); only IonInput import was removed. Always check diff context (leading space vs +/-) before attributing a build warning to a story.
2026-06-26 — frontend/rxjs — When converting as-you-type searches to debounced switchMap pipes, FRED pattern is: per-component `searchInput$ = new Subject<string>()` fed by `onSearchInput($event)` via `.next()`, pipe `debounceTime(300)→distinctUntilChanged()→switchMap(forkJoin equity+etf inner obs with catchError INSIDE)→takeUntil(destroy$)`; also remove the template `debounce="300"` to avoid double-debounce.
- 2026-06-28 — angular/directive — Standalone attribute directives applied to `<ion-content>` must be in the host component's `imports:[]` array; the directive receives the host `IonContent` via DI (constructor injection) — no `@ViewChild` needed. The directive file lives in `src/app/directives/`; import path is `../directives/...` for top-level feature folders (`app/<feature>/`) and `../../directives/...` for `app/components/*` and `app/pages/*`.
- 2026-06-28 — verifier/keyboard-avoid — `angular.json` has NO `development` build config (configs are production/local/dev/test/ci); `ng build --configuration development` exits 127 ("Configuration 'development' ... is not set"), which a wrapping shell can mask as exit 0 — use `--configuration dev` (keeps AOT: the `@angular-devkit/build-angular:application` builder is always-AOT; dev only flips optimization/sourcemap). A green AOT build with no "Can't bind to 'appKeyboardAvoid'" error is positive proof every consuming standalone component added the directive to its `imports:[]`. Native keyboard scroll is device-only (Capacitor Keyboard never fires on web → directive no-ops; the web global focusin fallback in app.component.ts handles it), so behavioral acceptance is inherently un-runnable in any non-device env — not a sandbox gap.
- 2026-06-28 — npm/lockfile-churn — npm 10.x writes `os`/`cpu`/`libc` fields for optional native deps (rollup, esbuild); the exact fields differ by npm patch version. Pin `"packageManager": "npm@10.9.7"` in both package.json files + `npm install -g npm@10.9.7` in CI before `npm ci` to make all three environments emit identical lockfiles. npm 10.9.7 does NOT write `libc` for rollup/esbuild packages (26 lines removed from the frontend lockfile on first regen; idempotent thereafter).
- 2026-06-28 — npm/lockfile — npm 10.x writes platform `os`/`cpu`/`libc` fields into package-lock.json optionalDependencies; the emitted set varies by npm patch version, causing PR diff churn. Fix is to PIN one npm version (packageManager field + CI `npm install -g npm@X`), NOT strip the fields — stripping breaks rollup/esbuild native-binary resolution. Verify idempotency by regen-on-top-of-regen producing zero diff (DEV-198).
